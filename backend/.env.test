NODE_ENV=test

# ⚠️  BASE DE DATOS DE PRUEBA SEPARADA
# Cambia el nombre de la DB por una exclusiva para tests.
# NUNCA uses aquí la misma DB que en desarrollo.
DATABASE_URL="postgresql://postgres:CambiarContrasen@80@localhost:5432/b2b_db_test?schema=public"

# JWT (puede ser el mismo secret, solo se usa en tests)
JWT_SECRET="test_secret_key_for_tests_only"
JWT_EXPIRES_IN="1d"

PORT=3001
